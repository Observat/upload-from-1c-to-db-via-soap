Процедура ЗапускИзСправочникаВнешнихОбработок() Экспорт
  НазваниеВнешнейОбработки = "test2";

  // TODO Исключение можно. Но если не перехватывать, то всё-равно напишет ошибку в ЖурналРегистрации и аварийно завершит обработку.
  ТекСсылка = Справочники.ВнешниеОбработки.НайтиПоНаименованию( НазваниеВнешнейОбработки );
  ИмяФайла  = ПолучитьИмяВременногоФайла();
  ДвоичныеДанные = ТекСсылка.ХранилищеВнешнейОбработки.Получить();
  ДвоичныеДанные.Записать(ИмяФайла);

  ВнешняяОбработка = ВнешниеОбработки.Создать(ИмяФайла, ЛОЖЬ);
  ВнешняяОбработка.ВыгрузитьЧерезSOAP();  // "ВыгрузитьЧерезSOAP" - это функции с "экспорт" в ВнешняяОбработка

  УдалитьФайлы(ИмяФайла);
КонецПроцедуры
